/**
 * Shared theme constants — no framework boundary so this file is safe to
 * import in both server and client contexts.
 */
export const THEME_COOKIE_NAME = "rift-theme";
export const THEME_STORAGE_KEY = "rift-theme";
