/**
 * Shared theme constants — no "use client" boundary so this file is safe to
 * import in both Server Components and Client Components.
 */
export const THEME_COOKIE_NAME = "rift-theme";
export const THEME_STORAGE_KEY = "rift-theme";
